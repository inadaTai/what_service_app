<%= form_with(model: [micropost, comment], url: micropost_comments_path(@micropost) ) do |f| %>
    <%= f.text_area :body,  size: "70x4" %><br>
    <%= f.submit "コメントする", class: "btn btn-primary" %>
<% end %>
