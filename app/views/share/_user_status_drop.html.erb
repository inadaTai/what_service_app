<li>
  <img src="/color.png" width="250px">
  <% if user.picture.present? %>
    <%= image_tag user.resize_icon %><br>
  <% else %>
    <img src="/default2.jpg" width="50px" height="50px"><br>
  <% end %>
  <%= "ユーザー名：#{user.name}" %><br>
  <% if user.introduct.present? %>
    <%= "#{user.introduct}" %>
  <% else %>
    プロフィールが設定されていません
  <% end %>
  </li>